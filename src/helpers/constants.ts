export const URL_BACKEND = "http://demo4409479.mockable.io";

export enum RangeMode {
  NORMAL = "normal",
  RANGE = "range",
}

export enum RangePosition {
  START = "start",
  END = "end",
}

export enum RangeInputMode {
  LABEL = "label",
  INPUT = "input",
}
